%h1 Velocity
%p Our velocity for the past week is...
.well
  %h1.text-center
    %small= @velocity
%p Previous weeks
= javascript_include_tag "//www.google.com/jsapi", "chartkick"
= column_chart TaskMetric.where { done_at >= 12.weeks.ago }.group_by_week(:done_at, :format => '%m/%d/%Y').sum(:estimate)
.well
  .form-actions
    = link_to 'Looking for reports?', card_analytics_report_url
